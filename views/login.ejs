<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link type="text/css" rel="stylesheet" href="css/login.css" />
    <link rel="shortcut icon" href="/Images/favicon (2).ico" type="image/x-icon">
    <!-- CSS only -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.datatables.net/1.12.1/css/dataTables.bootstrap4.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <script
      src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
      integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"
      integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
      integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
      crossorigin="anonymous"
    ></script>
    <title>Advisoropedia Login Page</title>
  </head>
  <body>
    <div class="container">
      <div class="login">
          <h1>Login Form</h1>
          <form method="post" action="/login">
              <div class="input-box">
                  <input type="email" name="email" placeholder="Enter your E-mail id" required>
                  <i class="fa fa-envelope"></i>
              </div>

              <div class="input-box">
                  <input type="password" name="password" placeholder="Enter Password" required>
                  <i class="fa fa-lock"></i>
              </div>

              <div class="rembar">
                  <input id="rembar" type="checkbox">
                  <label for="rembar">remember me</label>
              </div>

              <button type="submit">LOGIN</button>
          </form>
      </div>
  </div>

  <script>
    // Function to set a cookie
    function setCookie(cname, cvalue, exdays) {
        var d = new Date();
        d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));
        var expires = "expires=" + d.toUTCString();
        document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
    }

    // Function to check if the "Remember Me" checkbox is checked
    function rememberMe() {
        var rembar = document.getElementById("rembar");
        return rembar.checked;
    }

    // Function to handle form submission
    function validateForm() {
        var email = document.getElementById("email").value;
        var password = document.getElementById("password").value;

        // If "Remember Me" is checked, set the cookie
        if (rememberMe()) {
            setCookie("rememberedEmail", email, 30); // Cookie expires in 30 days
        } else {
            // If "Remember Me" is not checked, clear the cookie
            setCookie("rememberedEmail", "", -1); // Clear the cookie
        }

        // Add your login validation logic here

        return true; // Return true to allow form submission
    }

    // Function to populate the email field if a cookie exists
    window.onload = function() {
        var rememberedEmail = getCookie("rememberedEmail");
        if (rememberedEmail != "") {
            document.getElementById("email").value = rememberedEmail;
        }
    }
</script>

  </body>
</html>
